use std::{
    collections::{BTreeMap, HashMap},
    num::ParseIntError,
};

mod db;

fn main() {
    let col = db::Column::new(true, db::ColumnContent::Str(BTreeMap::new()));
    col.insert(rv, v)
}
